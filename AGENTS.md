# このプロジェクトについて

広告無しの計算機アプリです。シンプルでわかりやすさがこのアプリの売りです。

# ブランチ戦略

github flowをベースとしていて、mainブランチをベースとして各種featureブランチが存在します。
リリースするときにだけreleaseブランチを切ります。

## リリース作業

他のreleaseブランチに倣って、release/{バージョン名}を切り、そこでアプリバージョンを上げるコミットを出します。
アプリバージョンは @app/build.gradle.kts の以下の箇所のVersionに渡している引数を変更してバージョンを上げます。

```kotlin
val version = Version(2, 0, 2, 0)
```

バージョンを上げるときは現在がどういうバージョンで、どのバージョンに上げるのかをAskUserQuestionで聞いてください。

バージョン変更作業が終わったらそのままコミットしてPRを出します。
PRはストア申請が終わって正式にリリース後にマージします。
